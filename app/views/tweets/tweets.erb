<h2>Welcome, <%= current_user.username %> Tweets</h2>

<% @tweets.each do |tweet| %>
  <div>
    <%= tweet.content %>
    <a href="/tweets/<%= tweet.id %>">View Tweet</a>
  </div>
<% end %>

<p><a href="/tweets/new">Create a new tweet!</a></p>
